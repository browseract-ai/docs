---
title: 快速开始
description: 用 stealth-extract 或完整浏览器会话在 60 秒内跑通第一次自动化。
---


两条路径，60 秒看到第一个结果。

## 路径 A：内容提取（WebFetch 的高级替代）

WebFetch / curl 拿不到的——JS 渲染、反爬封锁、地域限制——一条命令搞定：

```bash
browser-act stealth-extract https://example.com
```

返回 markdown 格式的页面内容。无需管理浏览器，无需会话，无需清理。带 JS 渲染和反爬绕过，且每次调用独立——多个 URL 可并行执行。

```bash
# HTML 格式
browser-act stealth-extract https://example.com --content-type html

# 用代理访问地域限制内容
browser-act stealth-extract https://example.com --dynamic-proxy JP

# 保存到文件
browser-act stealth-extract https://example.com --output ./page.md
```

适用：抓取受保护内容、批量数据采集、信息检索。详见 [反检测与封锁突破](/skill/anti-blocking)。

## 路径 B：完整浏览器自动化

需要登录、填表单、点击交互——使用会话：

```bash
# 1. 查看可用浏览器
browser-act browser list

# 2. 打开浏览器到目标 URL（启动会话）
browser-act --session my-task browser open <browser-id> https://example.com

# 3. 看页面有什么可交互元素
browser-act --session my-task state

# 4. 用元素索引交互
browser-act --session my-task click 4
browser-act --session my-task input 2 "hello@example.com"

# 5. 完成后关闭
browser-act session close my-task
```

## 核心循环

```
打开 → 状态 → 交互 → 状态 → ... → 关闭
```

1. **打开** browser open 启动会话
2. **状态** state 看页面元素（带索引）
3. **交互** click/input/select 用索引操作
4. **状态** 再看一次确认结果
5. 重复直到任务完成
6. **关闭** session close

## 理解 state 输出

`state` 是 Agent 的眼睛，返回 URL、标题和带索引的元素树：

```
url=https://example.com/login
title=Login

*[1]<div id=login-form />
  *[2]<input type=email placeholder=Email address />
  *[3]<input type=password placeholder=Password />
  *[4]<button id=submit />
    Sign In
  *[5]<a />
    Forgot password?
```

每个 `[N]` 是一个可交互元素，直接用索引操作：

```bash
browser-act --session login input 2 "user@example.com"
browser-act --session login input 3 "password123"
browser-act --session login click 4
```

页面变化后旧索引失效——再调用一次 `state` 取新索引。

## 命令链接

无依赖的连续操作用 `&&` 链接，一次提交：

```bash
browser-act --session s1 input 2 "user" && \
browser-act --session s1 input 3 "pass" && \
browser-act --session s1 click 4
```

需要读中间输出时（如先看 `state` 再决定点什么）分开执行。

## 下一步

| 想了解 | 去看 |
|--------|------|
| 三种浏览器形态如何选 | [三种浏览器形态](/skill/browser-modes) |
| 怎么对抗反爬 | [反检测与封锁突破](/skill/anti-blocking) |
| 卡住时怎么让人接管 | [更好的无头浏览器](/skill/headless) |
| 多任务并发怎么跑 | [并发与隔离](/skill/concurrency) |
| 完整命令有哪些 | [命令参考](/skill/commands) |
| 让 AI 替你写爬虫 | [Skill Forge](/skill/skill-forge) |
